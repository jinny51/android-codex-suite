interface:
  display_name: "AKBS Patch Submit"
  short_description: "Prepare and submit Android change packages"
  default_prompt: "Use $akbs-patch-submit to read, check, prepare, or submit a final Android change v2 package with my existing member profile; preserve legacy v1 compatibility, exact package bytes, canonical platform fields, and the common patch-upload lifecycle."

policy:
  allow_implicit_invocation: true
