# Optional decision-only provider.

[[skills]]
name = "jinny-android-coding-practices"
category = "Optional Practices"
summary = "返回不覆盖核心 android-change-policy 的 Jinny coding policy 决策"

[[skills]]
name = "jinny-android-execution-policy"
category = "Optional Practices"
summary = "返回受 controller rollout ceiling 约束且不直接执行的 execution policy 决策"
