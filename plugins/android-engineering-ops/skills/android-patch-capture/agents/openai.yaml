interface:
  display_name: "Android Patch Capture"
  short_description: "Package one coherent Android change and its evidence"
  default_prompt: "Use $android-patch-capture to acquire a fresh remote snapshot and atomically produce the final validated Android change v2 package for direct check, prepare, and submission by $akbs-patch-submit."
